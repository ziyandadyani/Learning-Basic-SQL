#Creates an in-memory SQLite database, creates departments and employees tables, populates them with sample data, 
#and executes an example query to retrieve employee names along with their department names.
